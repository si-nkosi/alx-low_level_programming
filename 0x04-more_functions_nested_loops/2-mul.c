#include "main.h"

/**
 * mul(a, b) - multiplies two integers
 * @a: First integer
 * @b: Second integer
 * Return: Multiplication result of 'a' and 'b'
 */
int mul(int a, int b)
{
	return (x * y);
}
